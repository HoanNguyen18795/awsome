apply plugin: "war"

bootWar {
    mainClassName = "com.hoannk.AwsomeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
